These files are source-controlled copies of the current NASUS host-side compose files.

Live host paths:

- `/volume1/docker/config/<service>/docker-compose.yml`
- `/volume1/docker/traefik/docker-compose.yml`

Repo paths:

- `host-configs/nasus/<service>/docker-compose.yml`
- `host-configs/nasus/traefik/docker-compose.yml`

Keep these copies in sync whenever NASUS host-side compose files change.
